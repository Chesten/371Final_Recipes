function Header(){
    return(
        <div >
            <p>
                this should be a constant header for all pages<br></br>
                No props needed, unless we want to send user name for welcome
            </p>
        </div>
    )
}

export default Header